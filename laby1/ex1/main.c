#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>
#include <my_library.h>

int main(int argc, char** argv){
    if(argc < 2){
        fprintf(stderr, "Invalid number of arguments");
        exit(0);
    }

    for(int i = 1; i < argc; i++){
        if(strcpm(argv[i], "create_table") == 0){

        }
        else if(strcpm(argv[i], "create_table") == 0)
        {
            
        }
        else if(strcpm(argv[i], "create_table") == 0)
        {
            
        }
        else if(strcpm(argv[i], "create_table") == 0)
        {
            
        }
        else
        {
            fprintf()
        }
        
    }
}